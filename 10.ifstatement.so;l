
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.10;

contract ifstement{
            string public mystring="the given value is grater then 10";
        

    function mystatement(uint x)external pure returns(uint){
        if(x%2==0){
            return 1;
        }
        else{
        return 2;
        }

    }
    // other method 

    function mystatements(uint _x)external pure returns(uint){

     return _x < 10 ? 1: 2;

    }
}
